{
    "steps": [
        {
            "displayName": "Authenticate",
            "dataStream": { "name": "regions" },
            "required": true,
            "error": "Could not authenticate with Civo. Check the API key is correct and active (Civo dashboard → Account → Settings → Security → API keys).",
            "success": "Connected to Civo successfully."
        }
    ]
}
