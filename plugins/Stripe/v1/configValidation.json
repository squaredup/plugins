{
    "steps": [
        {
            "displayName": "Authenticate",
            "dataStream": { "name": "accountBalance" },
            "required": true,
            "error": "Could not authenticate. Check your API key is correct and has at least read access to Balance.",
            "success": "Connected successfully."
        }
    ]
}
