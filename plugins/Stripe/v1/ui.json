[
    {
        "type": "password",
        "name": "apiKey",
        "label": "Secret API Key",
        "help": "A Stripe restricted or secret key (`rk_...` / `sk_...`) with read access. See the plugin documentation for how to create a read-only restricted key.",
        "validation": {
            "required": true,
            "pattern": "^(rk|sk)_(live|test)_[A-Za-z0-9]+$"
        },
        "placeholder": "e.g. rk_live_..."
    }
]
