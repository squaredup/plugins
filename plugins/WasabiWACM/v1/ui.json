[
    {
        "type": "text",
        "name": "username",
        "label": "Username",
        "help": "Your WACM account name (for an Account API Key) or WACM username (for a User API key).",
        "validation": {
            "required": true
        }
    },
    {
        "type": "password",
        "name": "apiKey",
        "label": "API Key",
        "help": "The Account API key or User API key generated in the WACM console — shown only once when created.",
        "validation": {
            "required": true
        }
    }
]
