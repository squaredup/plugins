[
    {
        "label": "Organization name",
        "name": "org",
        "placeholder": "Enter an organization name",
        "title": "Enter an organization name",
        "type": "text",
        "validation": {
            "maxLength": 128,
            "minLength": 1,
            "required": true
        }
    },
    {
        "help": "Provide a token to access private repositories. For fine-grained tokens, it must be created by an organization owner with access to the required repositories",
        "label": "Personal access token",
        "name": "accessToken",
        "placeholder": "Enter a personal access token",
        "title": "Enter a personal access token",
        "type": "password",
        "validation": {
            "required": false
        }
    }
]
