{
    "steps": [
        {
            "displayName": "Authenticate",
            "dataStream": { "name": "serverInfoAuthenticated" },
            "required": true,
            "error": "Could not authenticate with N-central. Check the Server URL and User-API Token are correct and that the token hasn't expired.",
            "success": "Connected successfully."
        }
    ]
}
