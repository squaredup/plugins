{
    "name": "serviceOrgs",
    "displayName": "Service Organizations",
    "description": "Service organizations, one row per organization",
    "tags": ["Organization"],
    "baseDataSourceName": "httpRequestUnscoped",
    "config": {
        "httpMethod": "get",
        "endpointPath": "api/service-orgs",
        "pathToData": "data",
        "paging": {
            "mode": "offset",
            "pageSize": { "realm": "queryArg", "path": "pageSize", "value": "200" },
            "offset": {
                "mode": "page",
                "rowCountIn": { "realm": "payloadArraySize", "path": "data" },
                "base": 1
            },
            "out": { "realm": "queryArg", "path": "pageNumber" }
        }
    },
    "matches": "none",
    "metadata": [{ "pattern": ".*" }],
    "timeframes": false,
    "visibility": { "type": "hidden" }
}
