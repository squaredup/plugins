{
    "name": "sites",
    "displayName": "Sites",
    "description": "Sites, one row per site",
    "tags": ["Organization"],
    "baseDataSourceName": "httpRequestUnscoped",
    "config": {
        "httpMethod": "get",
        "endpointPath": "api/sites",
        "pathToData": "data",
        "paging": {
            "mode": "offset",
            "pageSize": { "realm": "queryArg", "path": "pageSize", "value": "200" },
            "offset": {
                "mode": "page",
                "rowCountIn": { "realm": "payloadArraySize", "path": "data" },
                "base": 1
            },
            "out": { "realm": "queryArg", "path": "pageNumber" }
        }
    },
    "matches": "none",
    "metadata": [{ "pattern": ".*" }],
    "timeframes": false,
    "visibility": { "type": "hidden" }
}
