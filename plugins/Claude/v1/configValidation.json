{
    "steps": [
        {
            "displayName": "Authenticate",
            "dataStream": { "name": "organization" },
            "required": true,
            "error": "Could not authenticate. Ensure the key starts with sk-ant-admin and belongs to an organisation admin.",
            "success": "Connected successfully."
        },
        {
            "displayName": "Check workspace access",
            "dataStream": { "name": "workspaces" },
            "required": false,
            "error": "Authenticated but no workspaces found. The organisation may have no workspaces configured.",
            "success": "Workspaces accessible."
        }
    ]
}
