[
    {
        "type": "markdown",
        "name": "info",
        "content": "An **Admin API key** is required (starts with `sk-ant-admin...`). Standard API keys cannot access usage or cost data. Create one at [Claude Console → Settings → Admin Keys](https://console.anthropic.com/settings/admin-keys)."
    },
    {
        "type": "password",
        "name": "adminApiKey",
        "label": "Admin API Key",
        "validation": { "required": true },
        "help": "Your Anthropic Admin API key (starts with `sk-ant-admin...`). Only organisation admins can create these."
    }
]
