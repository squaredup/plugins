[
    {
        "type": "text",
        "name": "baseUrl",
        "label": "Grafana URL",
        "placeholder": "https://your-grafana.example.com",
        "help": "Enter the base URL of your Grafana instance without a trailing slash.",
        "validation": {
            "required": true,
            "pattern": {
                "value": "^https?://[^\\s/]+(:\\d+)?(/[^\\s]*[^/\\s])?$",
                "message": "Please enter a valid URL without a trailing slash (e.g. https://grafana.example.com)"
            }
        }
    },
    {
        "type": "password",
        "name": "serviceAccountToken",
        "label": "Service account token",
        "placeholder": "glsa_xxxxxxxxxxxxxxxxxxxx",
        "help": "Generate a token for a service account with the Viewer role. See [Grafana service accounts](https://grafana.com/docs/grafana/latest/administration/service-accounts/) for instructions.",
        "validation": {
            "required": true
        }
    }
]
