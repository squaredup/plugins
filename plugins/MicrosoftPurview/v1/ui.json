[
    {
        "type": "text",
        "name": "accountName",
        "label": "Purview account name",
        "placeholder": "e.g. contoso-pv",
        "help": "The short name of your Purview account — the part before `.purview.azure.com`. Do **not** include `https://` or the full domain.",
        "validation": {
            "required": true
        }
    },
    {
        "type": "text",
        "name": "tenantId",
        "label": "Directory (tenant) ID",
        "placeholder": "Enter a directory (tenant) ID",
        "validation": {
            "required": true
        }
    },
    {
        "type": "text",
        "name": "clientId",
        "label": "Application (client) ID",
        "placeholder": "Enter an application (client) ID",
        "validation": {
            "required": true
        }
    },
    {
        "type": "password",
        "name": "clientSecret",
        "label": "Client secret",
        "placeholder": "Enter a client secret",
        "validation": {
            "required": true
        }
    }
]
