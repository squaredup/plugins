{
    "name": "searchAssets",
    "displayName": "Search Assets",
    "description": "Search the Purview catalog with optional keyword, entity-type, and classification filters",
    "tags": ["Assets"],
    "baseDataSourceName": "httpRequestUnscoped",
    "ui": [
        {
            "type": "text",
            "name": "keywords",
            "label": "Keywords",
            "placeholder": "e.g. sales report"
        },
        {
            "type": "text",
            "name": "entityType",
            "label": "Entity Type",
            "placeholder": "e.g. azure_blob_path"
        },
        {
            "type": "text",
            "name": "classification",
            "label": "Classification",
            "placeholder": "e.g. MICROSOFT.GOVERNMENT.US.SOCIAL_SECURITY_NUMBER"
        },
        {
            "type": "number",
            "name": "limit",
            "label": "Result Limit",
            "defaultValue": 100,
            "validation": { "min": 1, "max": 1000 }
        }
    ],
    "config": {
        "httpMethod": "post",
        "endpointPath": "datamap/api/search/query",
        "getArgs": [
            { "key": "api-version", "value": "2023-09-01" }
        ],
        "postBody": {
            "keywords": "{{ keywords || null }}",
            "limit": "{{ Math.min(Math.max(Number(limit) || 100, 1), 1000) }}",
            "filter": "{{ (entityType || classification) ? { and: [entityType ? { entityType: entityType } : null, classification ? { classification: classification } : null].filter(Boolean) } : null }}"
        },
        "postRequestScript": "searchAssets.js"
    },
    "manualConfigApply": true,
    "matches": "none",
    "metadata": [
        { "name": "id", "displayName": "Asset GUID", "shape": "guid", "visible": false },
        { "name": "name", "displayName": "Name", "role": "label" },
        { "name": "qualifiedName", "displayName": "Qualified Name" },
        { "name": "entityType", "displayName": "Entity Type" },
        { "name": "assetType", "displayName": "Asset Type" },
        { "name": "classification", "displayName": "Classifications" },
        { "name": "term", "displayName": "Glossary Terms" }
    ],
    "timeframes": false
}
