[
    {
        "type": "text",
        "name": "publicKey",
        "label": "Public Key",
        "help": "The public API key from the Datto Partner Portal: **Admin → Integrations → API Keys**. Sent as the username for HTTP Basic authentication.",
        "validation": { "required": true }
    },
    {
        "type": "password",
        "name": "secretKey",
        "label": "Secret Key",
        "allowEncryption": true,
        "help": "The secret API key generated alongside the public key. Shown only once when the key is created — regenerate the key pair if it has been lost.",
        "validation": { "required": true }
    }
]
