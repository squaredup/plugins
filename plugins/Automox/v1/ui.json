[
    {
        "type": "password",
        "name": "apiKey",
        "label": "API Key",
        "help": "An Automox API key from **Settings → Keys** in the Automox console.",
        "validation": { "required": true }
    },
    {
        "type": "number",
        "name": "organizationId",
        "label": "Organization ID",
        "help": "The numeric organization ID (`o`) shown in the Automox console's URL when this organization is selected.",
        "validation": { "required": true }
    }
]
