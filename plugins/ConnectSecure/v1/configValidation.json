{
    "steps": [
        {
            "displayName": "Authenticate",
            "dataStream": {
                "name": "companies",
                "config": { "paging": { "pageSize": { "value": "1" } } }
            },
            "required": true,
            "error": "Could not authenticate. Check your Pod Base URL, Tenant Name, Client ID, Client Secret and API User ID are correct.",
            "success": "Connected successfully."
        }
    ]
}
