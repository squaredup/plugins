{
    "steps": [
        {
            "displayName": "Authenticate",
            "dataStream": { "name": "systemStatus" },
            "required": true,
            "error": "Could not connect to PRTG. Check the PRTG URL is reachable from SquaredUp, and that the API key is correct and has not been deleted.",
            "success": "Connected to PRTG successfully."
        },
        {
            "displayName": "Time zone",
            "dataStream": {
                "name": "timeZoneCheck",
                "config": { "errorOnEmptyResults": true }
            },
            "required": false,
            "error": "The PRTG time zone selected does not match the one PRTG reports. Compare it against Setup → Account Settings → My Account → Time Zone in PRTG — until they agree, Log and Sensor History will be shifted by the difference.",
            "success": "Time zone matches PRTG."
        }
    ]
}
